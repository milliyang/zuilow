# Real trading stack (real). Copy to .env and adjust.
# Ports: project-prefixed (ZUILOW_PORT, PPT_PORT). DMS external (DMS_BASE_URL). No stime.
# REPO_ROOT is set by deploy_real.sh; set here if you run docker compose manually.

ZUILOW_PORT=11180
PPT_PORT=11182

# Shared webhook token (optional: set in .env to override default)
# WEBHOOK_TOKEN=real_webhook_shared_token

# DMS is external. Host must be reachable from containers.
# - Real 与 DMS 同机（如都在 mongkok）：用 host.docker.internal，避免容器内 mongkok 解析成 127.0.0.1 连到自己
# - DMS 在另一台机：填那台机 hostname/IP，如 http://otherhost:11183
#DMS_BASE_URL=http://host.docker.internal:11183
DMS_BASE_URL=http://mongkok:11183
# When DMS has DMS_API_KEY set, set the same value here. Leave empty if DMS has no API key.
DMS_API_KEY=your-shared-secret

#
REPO_ROOT=/path/to/sai
