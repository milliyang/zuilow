# ZuiLow Web Service Environment
# 复制为 .env 并填入实际值。不设置 SECRET_KEY 时每次重启会生成新密钥，登录状态会丢失。

# Flask：必须设置固定值，否则每次重启需重新登录
# 生成: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-in-production

# Server
HOST=0.0.0.0
PORT=11180

# Logging
LOG_LEVEL=INFO
