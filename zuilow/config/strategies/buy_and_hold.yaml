# Buy and hold strategy (config-driven)
strategy: BuyAndHold
description: Buy on first bar, hold to end

params:
  reason: "buy and hold"
  only_first: true   # true = at most one buy per run; false = buy every symbol with no position

# Risk (optional, for future use)
# risk:
#   max_position: 10000
