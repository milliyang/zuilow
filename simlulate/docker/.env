# Full-stack simulation (simlulate). Copy to .env and adjust.
# 端口约定：zuilow 11180, ppt 11182, DMS 11183（外部）, stime 11185
# REPO_ROOT: 仓库根（含 zuilow 的目录）。deploy_sim.sh 会自动设置；手动 docker compose 时在此设置。
# 用于挂载 nn（${REPO_ROOT}/../nn -> /workspace/nn），供 ZuiLow 的 GRL 等策略使用；不设则 nn 不挂载。

# Stime (simulation time service). Tick 目标由 compose 里 TICK_URLS 写死，无需在 .env 配置。
STIME_PORT=11185

# ZuiLow (in Docker: SIMULATION_TIME_URL, PAPER_TRADE_URL, WEBHOOK_TOKEN, DMS_BASE_URL set by compose)
ZUILOW_PORT=11180

# PPT (in Docker: SIMULATION_MODE, SIMULATION_TIME_URL, DMS_BASE_URL, WEBHOOK_TOKEN set by compose)
PPT_PORT=11182
# PPT 从 DMS 取价；与 ZuiLow 共用 DMS_BASE_URL（compose 默认 host.docker.internal:11183）

# Shared webhook token (optional: set in .env to override default)
# WEBHOOK_TOKEN=sim_webhook_shared_token

# DMS is external: set to your DMS base URL (host must be reachable from containers).
# Use a hostname or IP that containers can resolve (e.g. LAN host: http://mongkok:11183).
DMS_BASE_URL=http://mongkok:11183
# When DMS has DMS_API_KEY set, set the same value here so PPT can get quotes (X-API-Key). Leave empty if DMS has no API key.
DMS_API_KEY=your-shared-secret

# 示例：若仓库在 /home/you/work/quant/sai，则 REPO_ROOT=/home/you/work/quant/sai，nn 为 /home/you/work/quant/nn
REPO_ROOT=/home/leo/work/quant/sai
PYTORCH_CUDA_AVAILABLE=True
DOCKER_BUILDKIT=1
