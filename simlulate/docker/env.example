# Full-stack simulation (simlulate). Copy to .env and adjust.
# This file is the single place to set ports and shared env for all services (project-prefixed names).

# ========== Ports (one place; each app reads its own PORT env) ==========
# ZuiLow, PPT, Stime are in this stack; DMS/One are external or separate.
ZUILOW_PORT=11180
PPT_PORT=11182
STIME_PORT=11185
# DMS (external): set DMS_BASE_URL below; DMS app uses DMS_PORT (default 11183).
# One (other project): ONE_PORT=11181

# ========== Shared / service-specific ==========
# REPO_ROOT: deploy_sim.sh sets it; for manual compose set here (e.g. /home/you/work/quant/sai). Used for nn mount.
REPO_ROOT=/home/you/work/quant/sai

# Webhook token (ZuiLow/PPT; override default)
# WEBHOOK_TOKEN=sim_webhook_shared_token

# When QUANT_ROOT is set and dir exists, env.sh sets INSTALL_PYTORCH=true so ZuiLow image gets PyTorch + grl/sort deps.
# QUANT_ROOT=   (empty = no PyTorch/grl)
# PYTORCH_CUDA_AVAILABLE: only when INSTALL_PYTORCH=true. True=CUDA 12.1, False=CPU.
PYTORCH_CUDA_AVAILABLE=False
DOCKER_BUILDKIT=0

# DMS (external): base URL reachable from containers (e.g. http://host.docker.internal:11183 or http://mongkok:11183).
DMS_BASE_URL=http://host.docker.internal:11183
# When DMS uses X-API-Key, set the same here.
DMS_API_KEY=your-shared-secret
